# 第 05 项证明材料截图建议

建议上传以下 5 张图，能比较完整地证明项目存在且具备 Agent 链路：

1. `http://127.0.0.1:8001/docs` 的接口文档截图，展示 `/api/v1/process_zip` 和 `/api/v1/process_one`。
2. 上传 `sample_data/henan_museum_demo.zip` 后的返回结果截图。
3. `runtime/demo_result.json` 中 8 种风格讲解词的结果截图。
4. `runtime/audio/` 中生成音频文件的截图，或浏览器打开音频 URL 的截图。
5. `runtime/sync_records.jsonl` 中模拟业务回写记录截图。

如果有真实业务后台，可以额外上传后台中已写入文本、音频 URL、审核状态的页面截图。
